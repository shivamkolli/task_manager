<div id="flash" class="fixed right-6 top-6 z-[100] space-y-2">
  <% flash.each do |type, message| %>
    <div
      class="rounded-lg bg-gray-900 px-5 py-3 text-sm text-white shadow-lg"
    >
      <%= message %>
    </div>
  <% end %>
</div>